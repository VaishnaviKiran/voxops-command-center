server:
  port: 8082

spring:
  application:
    name: voice-stream-service

incident:
  service:
    base-url: http://localhost:8081
    internal-token: ${VOXOPS_INTERNAL_SERVICE_TOKEN:local-dev-internal-token}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
